// Plantilla opcional para el nodo indoor.
// Copiá a secrets.h solo si necesitás overrides (hostname / OTA futuro).
// No versionar secrets.h. WiFi se configura con WiFiManager (portal AP).

#pragma once

// #define HOSTNAME "weather-indoor-01"

// OTA (no implementado en v0.1; reservado):
// #define OTA_PASSWORD "cambia-esto"
